import React from 'react';
import ProfileClient from './ProfileClient';

export const metadata = {
  title: 'Mon Profil — Brunch Bouaké PMS',
};

export default function ProfilePage() {
  return (
    <div>
      <ProfileClient />
    </div>
  );
}
